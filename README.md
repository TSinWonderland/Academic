# Personal_Expense_Tracker

#This is an interactive menu
#The code will prompt user for- expense details date, category, cost, and a description
#Next, the user can view their expenses
#Then the code creates a budget tracker for the user- it both sets the budget based on users input and then tracks the expenses against the budget based off what is entered
#All data will be saved in the "Expense.csv" file which is automatically created based off the code
